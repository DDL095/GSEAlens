% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_code_generator.R
\name{generate_combined_plot_code}
\alias{generate_combined_plot_code}
\title{Generate Combined Pathway Plotting Code}
\usage{
generate_combined_plot_code(
  gsea_res_var = "gsea_res",
  contrast_id,
  target_pathways,
  subPlot = 3,
  curve_colors = NULL,
  left_group = "Left",
  right_group = "Right"
)
}
\arguments{
\item{gsea_res_var}{Character. Name of the GseaRes variable the generated
code should reference (default \code{"gsea_res"}). The user replaces the
placeholder assignment with their own object.}

\item{contrast_id}{Character. Contrast ID passed to
\code{extract_gsea_task()}.}

\item{target_pathways}{Character vector. Pathway IDs to plot.}

\item{subPlot}{Integer (1-3). \code{plot_directional_gsea} subPlot argument
(1 = running score, 2 = rank position, 3 = both). Default 3.}

\item{curve_colors}{Character vector of color hex codes for pathway curves.
If NULL, falls back to a sensible default palette.}

\item{left_group}{Character. Left group label for axis annotation.}

\item{right_group}{Character. Right group label.}
}
\value{
Character scalar of executable R code.
}
\description{
Return an executable R script reproducing the Combined Pathway
  Plotting module (mod_multi_plot / module 12) as a static figure. Mirrors
  the interactive \code{plot_directional_gsea()} call the app performs when
  the user selects pathways in the main table's "Combined Display" column.
}
\examples{
code <- generate_combined_plot_code(
  contrast_id    = "untrt_vs_trt",
  target_pathways = c("HALLMARK_TNFA_SIGNALING_VIA_NFKB")
)
cat(code)
}
