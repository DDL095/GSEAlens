% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-codegen.R
\name{generate_plot_code}
\alias{generate_plot_code}
\title{生成 GSEA 绘图代码}
\usage{
generate_plot_code(
  contrast_id,
  pathway_ids,
  plot_subtype = 3,
  custom_colors = NULL,
  layout_mode = "auto_grid",
  custom_nrow = NULL,
  custom_ncol = NULL,
  plot_width = 12,
  plot_height = 9,
  plot_dpi = 300,
  rds_path = "path/to/your/GSEA_Capsule.rds"
)
}
\arguments{
\item{contrast_id}{字符串，对比组 ID（如 "IBAA_vs_PREA"）。}

\item{pathway_ids}{字符向量，通路 ID 列表。}

\item{plot_subtype}{整数，图像类型（1=仅富集图，2=富集+热图，3=完整带Rank）。}

\item{custom_colors}{字符向量，自定义颜色代码。}

\item{layout_mode}{字符串，排版模式（auto_grid/single_col/two_col/custom_grid）。}

\item{custom_nrow}{整数，自定义行数（可选）。}

\item{custom_ncol}{整数，自定义列数（可选）。}

\item{plot_width}{数值，图像宽度（英寸）。}

\item{plot_height}{数值，图像高度（英寸）。}

\item{plot_dpi}{整数，图像分辨率。}

\item{rds_path}{字符串，GseaRes 对象文件路径（用于代码中的注释）。}
}
\value{
字符串，格式化后的 R 代码。
}
\description{
生成完整的、可直接运行的 R 脚本，用于复现当前的 GSEA 可视化。
}
\examples{
\dontrun{
code <- generate_plot_code(
  contrast_id = "IBAA_vs_PREA",
  pathway_ids = c("HALLMARK_OXIDATIVE_PHOSPHORYLATION", "KEGG_GLYCOLYSIS"),
  plot_subtype = 3,
  custom_colors = c("#E41A1C", "#377EB8")
)
cat(code)
}
}
