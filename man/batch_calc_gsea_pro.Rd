% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_gsea_calculation.R, R/gsea_capsule.R
\name{batch_calc_gsea_pro}
\alias{batch_calc_gsea_pro}
\title{并行计算 GSEA 核心引擎 (Pro - 智能缓存版)}
\usage{
batch_calc_gsea_pro(
  gsea_env,
  custom_series_name = "Auto_Analysis",
  output_dir = "./GSEA_Output",
  force = FALSE,
  bidirectional = TRUE,
  workers = 20,
  minGSSize = 10,
  maxGSSize = 500,
  pvalueCutoff = 1
)

batch_calc_gsea_pro(
  gsea_env,
  custom_series_name = "Auto_Analysis",
  output_dir = "./GSEA_Output",
  force = FALSE,
  bidirectional = TRUE,
  workers = 20,
  minGSSize = 10,
  maxGSSize = 500,
  pvalueCutoff = 1
)
}
\arguments{
\item{gsea_env}{由 setup_gsea_env_pro() 创建的计算胶囊}

\item{custom_series_name}{自定义分析系列名称}

\item{output_dir}{输出目录}

\item{force}{是否强制重新计算}

\item{bidirectional}{逻辑值，是否进行双向对比，默认 TRUE}

\item{workers}{整数，手动指定使用的 CPU 核心数。如果为 NULL，则自动探测并保留 4 核。}

\item{minGSSize}{基因集最小包含基因数，默认 10}

\item{maxGSSize}{基因集最大包含基因数，默认 500}

\item{pvalueCutoff}{GSEA 的 P 值阈值（强制默认 1，保留全貌用于后续过滤）}

\item{top_n}{topTable 提取基因数，默认 Inf}

\item{seed}{随机种子，默认 123}

\item{future_maxsize_gb}{future 允许传输对象上限（GB），默认 32}
}
\value{
DudaliGseaResPro 对象

返回 DudaliGseaResPro 对象，包含元数据和所有计算结果

DudaliGseaResPro 对象

返回 DudaliGseaResPro 对象
}
\description{
结合原生代码最稳健的 t 值去重与大小写转换逻辑，支持硬件核数自定与双向翻转计算。

自动感知硬件多核算力，对封装在胶囊中的所有对比组（支持双向真实计算，无翻转作弊）进行严格的 GSEA 置换检验。

基于 topTable + SYMBOL + t 值 构建 geneList，
支持真实双向计算，并将结果封装为 DudaliGseaResPro。

结合原生代码最稳健的 t 值去重与大写转换逻辑，支持硬件核数自定与双向翻转计算。
}
