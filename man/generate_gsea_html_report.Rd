% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_gsea_visualization.R, R/gsea_capsule.R
\name{generate_gsea_html_report}
\alias{generate_gsea_html_report}
\title{Generate GSEA HTML Report Bundle (Pro 融合修复版)}
\usage{
generate_gsea_html_report(
  res_obj,
  output_base_dir = "GSEA_Results",
  p_adjust_cutoff = 1,
  top_plots = c(15, 15),
  save_pdf = FALSE,
  dge_list = NULL,
  dpi = 200
)

generate_gsea_html_report(
  res_obj,
  output_base_dir = "GSEA_Results",
  p_adjust_cutoff = 1,
  top_plots = c(15, 15),
  save_pdf = FALSE,
  dge_list = NULL,
  dpi = 200
)
}
\arguments{
\item{res_obj}{A completed DirectionalGSEA object from your pipeline.}

\item{output_base_dir}{The parent directory where the results bundle will be saved}

\item{p_adjust_cutoff}{Significance threshold for filtering pathways (Default: 1, keeps all pathways)}

\item{top_plots}{A numeric vector c(pos_N, neg_N) specifying how many top positive/negative pathways to plot. Set to c(0,0) to skip all plots. Set to c(Inf, Inf) for all.}

\item{save_pdf}{Logical, if TRUE, saves detailed plots as PDF as well as PNG.}

\item{dge_list}{Optional. An edgeR DGEList object to generate annotated expression heatmaps.}
}
\description{
恢复原有的热图逻辑、HTML子网页、高亮红蓝表格，并严格采用 200 DPI PNG。

Creates an independent bundle with main report, optionally generating detailed dashboards (Plots + Heatmaps) for top pathways to save time.

去除冗余文件夹嵌套，将 p.adjust/qvalue 引入表格，并强制 Description 在最后一列。

恢复原有的热图逻辑、HTML子网页、高亮红蓝表格，并严格采用 200 DPI PNG。
}
