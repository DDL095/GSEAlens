% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_code_generator.R
\name{generate_dotplot_code}
\alias{generate_dotplot_code}
\title{Generate Publication DotPlot Code}
\usage{
generate_dotplot_code(
  plot_df,
  color_mode = "padj",
  size_mode = "core_size",
  left_group = "Left",
  right_group = "Right",
  palette = "D",
  point_range = c(3, 8),
  base_size = 12
)
}
\arguments{
\item{plot_df}{data.frame with at least columns: ID, NES, p.adjust,
pvalue, CoreCount, setSize.}

\item{color_mode}{Character. One of "padj", "pval", "nes".}

\item{size_mode}{Character. One of "core_size", "setsize".}

\item{left_group}{Character. Left contrast label.}

\item{right_group}{Character. Right contrast label.}

\item{palette}{Character. Viridis option letter ("A"-"H").}

\item{point_range}{Numeric length 2. Min/max point size in mm.}

\item{base_size}{Numeric. theme_bw base font size.}
}
\value{
Character scalar of executable R code.
}
\description{
Return an executable ggplot2 script that reproduces the
  Pathway Relationship module's interactive DotPlot as a static,
  publication-ready figure. The data frame passed in is serialized as a
  \code{dput()}-style structure literal so the script is self-contained.
}

\examples{

# Construct a minimal pathway data.frame inline.

mini_df <- data.frame(

  ID = c("PATHWAY_A", "PATHWAY_B", "PATHWAY_C"),

  NES = c(1.5, -1.2, 0.8),

  pvalue = c(0.001, 0.01, 0.05),

  p.adjust = c(0.02, 0.04, 0.10),

  CoreCount = c(10L, 8L, 5L),

  setSize = c(50L, 40L, 30L)

)

code <- generate_dotplot_code(mini_df)

cat(code)

}
