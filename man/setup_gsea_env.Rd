% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_setup.R
\name{setup_gsea_env}
\alias{setup_gsea_env}
\title{初始化 GSEA 环境对象}
\usage{
setup_gsea_env(fit, pathway_obj, expr_data = NULL, target_factor = NULL)
}
\arguments{
\item{fit}{差异分析对象。支持 \code{MArrayLM} (limma) 或 \code{DESeqDataSet} (DESeq2)。}

\item{pathway_obj}{基因集对象。通常由 \code{build_gsea_pathways()} 生成。}

\item{expr_data}{可选。表达矩阵或 DGEList。若为 NULL，将尝试从 fit 对象中提取 (仅部分支持)。}

\item{target_factor}{仅用于 DESeq2。指定感兴趣的目标因子。若为 NULL，自动推断为设计公式最后一项。}
}
\value{
返回一个类为 \code{GseaEnv} 的标准化对象。
}
\description{
统一入口，支持 limma-voom (无截距设计) 和 DESeq2 对象。
自动提取对比组、差异分析结果和表达矩阵，生成标准化的 GseaEnv 对象。
}
\examples{
\dontrun{
# Limma 流程
design <- model.matrix(~ 0 + group, data = samples)
fit <- lmFit(expr, design) \%>\% contrasts.fit(cont.matrix) \%>\% eBayes()
env <- setup_gsea_env(fit, pathway_obj)

# DESeq2 流程
dds <- DESeqDataSetFromMatrix(countData, colData, design = ~ batch + group)
dds <- DESeq(dds)
env <- setup_gsea_env(dds, pathway_obj, target_factor = "group")
}
}
